export enum ImagePosition {
  LEFT = 'left',
  RIGHT = 'right',
}

export enum ImageSize {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}

export enum ArticleMode {
  CARD = 'card',
  DETAIL = 'detail',
}
